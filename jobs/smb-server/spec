---
name: smb-server

templates:
  pre-start.erb: bin/pre-start
  ctl.erb: bin/smb-server_ctl
  health_check.erb: bin/health_check

packages: []

properties:
  users:
    description: "SMB users objects, username: password"
    default: {}
  shares:
    description: "SMB shares objects, id: {writable, ....}"
    default: {}
